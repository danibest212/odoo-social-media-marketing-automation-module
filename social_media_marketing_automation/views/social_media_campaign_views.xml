<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_social_media_campaign_list" model="ir.ui.view">
        <field name="name">social.media.campaign.list</field>
        <field name="model">social.media.campaign</field>
        <field name="arch" type="xml">
            <list string="Social Media Campaigns">
                <field name="name"/>
                <field name="date_start"/>
                <field name="date_end"/>
                <field name="user_id"/>
                <field name="post_count"/>
                <field name="state" widget="badge" decoration-success="state == 'done'" decoration-info="state == 'in_progress'"/>
            </list>
        </field>
    </record>

    <record id="view_social_media_campaign_form" model="ir.ui.view">
        <field name="name">social.media.campaign.form</field>
        <field name="model">social.media.campaign</field>
        <field name="arch" type="xml">
            <form string="Social Media Campaign">
                <header>
                    <button
                        name="action_start"
                        string="Start"
                        type="object"
                        class="oe_highlight"
                        invisible="state != 'draft'"
                    />
                    <button
                        name="action_done"
                        string="Mark Done"
                        type="object"
                        class="oe_highlight"
                        invisible="state != 'in_progress'"
                    />
                    <button
                        name="action_cancel"
                        string="Cancel"
                        type="object"
                        invisible="state in ('done', 'cancelled')"
                    />
                    <button
                        name="action_reset_to_draft"
                        string="Reset to Draft"
                        type="object"
                        invisible="state == 'draft'"
                    />
                    <field name="state" widget="statusbar" statusbar_visible="draft,in_progress,done"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button
                            name="action_view_posts"
                            type="object"
                            class="oe_stat_button"
                            icon="fa-file-text-o"
                        >
                            <field name="post_count" widget="statinfo" string="Posts"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Campaign name"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="user_id"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                        <group>
                            <field name="date_start"/>
                            <field name="date_end"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description" name="description">
                            <field name="description"/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="view_social_media_campaign_search" model="ir.ui.view">
        <field name="name">social.media.campaign.search</field>
        <field name="model">social.media.campaign</field>
        <field name="arch" type="xml">
            <search string="Search Campaigns">
                <field name="name"/>
                <field name="user_id"/>
                <filter string="My Campaigns" name="my_campaigns" domain="[('user_id', '=', uid)]"/>
                <filter string="In Progress" name="in_progress" domain="[('state', '=', 'in_progress')]"/>
                <group expand="0" string="Group By">
                    <filter string="Status" name="group_state" context="{'group_by': 'state'}"/>
                    <filter string="Responsible" name="group_user" context="{'group_by': 'user_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_social_media_campaign" model="ir.actions.act_window">
        <field name="name">Campaigns</field>
        <field name="res_model">social.media.campaign</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first social media campaign
            </p>
            <p>
                Group related posts together to plan and track a marketing push.
            </p>
        </field>
    </record>
</odoo>
